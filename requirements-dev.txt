# Development & test dependencies (pip fallback; pyproject.toml is canonical).
#
# The modern path is `uv sync` (installs the [dependency-groups] dev group from
# pyproject.toml + uv.lock). This file mirrors that group for pip users and CI.
-r requirements.txt
pytest==9.0.3
pytest-django>=4.9,<5
pytest-cov>=5.0
# Architecture enforcement: fail CI if the layer dependency rule is violated.
import-linter>=2.0,<3.0
# Property-based testing of the domain (e.g. the state machine).
hypothesis>=6.100,<7.0
# Lint + format.
ruff>=0.6,<1.0
# Type checking (strict on the framework-free core) + editor import resolution.
mypy>=1.11,<2.0
django-stubs[compatible-mypy]>=5.1
djangorestframework-stubs[compatible-mypy]>=3.15
pyright>=1.1.380
# Git hooks.
pre-commit>=3.7
# Security scanning.
bandit>=1.7
pip-audit>=2.7
# Automated versioning + changelog + releases.
python-semantic-release>=9.8
